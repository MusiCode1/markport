# ‏קו ה-restructure (R) — ‏מפת-דרכים

> **‏מה זה**: ‏רצף הסלייסים של המעבר ל-TypeScript מודולרי עם ליבה טהורה,
> ‏ברמת **‏החלטות והיקף** ‏בלבד.
> **‏מה זה לא**: ‏בריף. ‏אין כאן נתיבי-קבצים, ‏חתימות, ‏טבלאות-טסטים ‏או פקודות DoD.
> **‏למה**: ‏אותו רציונל של `docs/plans/sync/ROADMAP.md` — ‏בריף שואב את ערכו מדיוק,
> ‏ודיוק הוא בדיוק מה שנרקב. ‏מפת-דרכים לא נרקבת באותו קצב.
> **‏איך משתמשים**: ‏רגע לפני dispatch ‏כותבים בריף מלא **‏מתוך** ‏הרשומה כאן, ‏ומאמתים אותו אז.
>
> **‏תאריך**: 2026-08-02 · **‏רציונל R0**: `docs/decisions/obsidian-web.md` (‏רשומת 2026-07-31)

---

## ‏הממצא שקובע את הרצף

**`@ow/core` ‏ניתן לצריכה היום רק ע"י Node.** ‏נמדד ב-2026-08-02:

| ‏מה | ‏הממצא |
|----|-------|
| ‏מה core ‏מייצא | `"exports": { "./fs": "./fs/index.ts" }` — **`.ts` ‏גולמי**. ‏אין שלב emit ‏בשום מקום; `bun run typecheck` ‏הוא `tsc --build` ‏לבדיקה בלבד, ‏והטסטים רצים ‏ב-`node --test "fs/*.test.ts"` (‏type-stripping ‏מקורי של Node). |
| ‏הדפדפן | `build-assets.sh` ‏עושה `cp -r "$MAIN_DIR/src/client-mobile"` — **‏אפס טרנספורמציה**. `index.html` ‏טוען **25 ‏סקריפטים קלאסיים** ‏בסדר ידני, ‏ומעיר בעצמו: *"Classic script (not `type="module"`) — index.html only supports classic scripts here"*. |
| ‏ה-Worker | `_worker.js/` ‏נבנה ‏ב-`cp` ‏של שני קבצים (‏צורת-תיקייה, Pages advanced mode). ‏אין bundling ‏בריפו. |
| ‏צרכני core ‏היום | `grep -rn '@ow/core' src/` → **‏אפס**. |

**‏המסקנה**: R1 ‏מחווט ארבעה אתרים — `sync-server`, `api/fs.js`, `system-plugins.js`,
`api/electron.js` — ‏**‏כולם Node**. ‏הוא יעבור ‏ירוק ‏בלי לגעת בחסם הזה בכלל.
‏הצרכן הראשון שאינו Node ‏ייתקע, ‏וכל הערך של "‏ליבה אחת לכל ה-hosts" ‏תלוי בו.

‏לכן **‏שאלת ה-emit ‏קודמת לכל סלייס-חילוץ שאינו Node** (R2 ‏למטה), ‏ולא נדחית
‏עד שייתקעו בה.

---

## ‏החלטות חוצות-סלייסים (‏סגורות)

1. **‏הליבה טהורה**: `packages/core` — TS, ‏אפס תלויות-ריצה, ‏אפס IO, ‏אפס מקורות-זמן.
   ‏הגבול נאכף ‏ע"י `scripts/check-core-boundary.mjs` (R0), ‏לא ‏ע"י משמעת.
2. **‏אפס שינוי התנהגות** ‏בכל סלייס-חילוץ — ‏כולל גוף-שגיאה, ‏קוד-שגיאה ‏וצורת-החוט.
   ‏זו דרישה מחייבת, ‏לא שאיפה. ‏ארבעה סבבי אימות ב-R1 ‏מצאו כאן רגרסיות שקטות
   ‏שאף טסט קיים לא תפס.
3. **‏חילוץ לפני המרה**: ‏קודם מזיזים לוגיקה **‏כפולה** ‏לליבה (‏שם הרווח מיידי ומדיד),
   ‏ורק אחר כך ממירים קבצים שלמים ל-TS. ‏המרה סיטונאית ‏של 10,000 ‏שורות JS ‏אינה ברצף.
4. **‏שער-רגרסיה לפני החיווט, ‏לא אחריו** (R1 Commit 0). ‏שער שנכתב אחרי השינוי
   ‏לא מוכיח דבר.
5. **legacy ‏לא נכנס לליבה** — `src/sync-server` ‏ו-`src/client-mobile/sync` ‏נשארים
   ‏כפי שהם (‏החלטה 9 ‏ב-`docs/plans/sync/ROADMAP.md`). ‏חילוץ מהם הוא עבודה מבוזבזת.
6. **`src/` ‏לא זז ל-`packages/`** ‏באף סלייס ברצף הזה. ‏זו החלטה נפרדת שלא נדונה.
7. **‏רצפת Node ‏אחת: 20.19+.** ‏ראה R2. ‏אין ‏הפרדה ‏בין ‏רצפת-ריצה ‏לרצפת-בנייה.
8. **‏בריפים ‏מקובעים ‏ב-git ‏גם ‏כטיוטות.** ‏זה ‏מה ‏שמאפשר ‏עבודה ‏לא-מלווה ‏ומדידה
   ‏של ‏מה ‏השתנה ‏בין ‏סבבי ‏אימות. (‏סוגר ‏גם ‏את ‏שאלה 5 ‏ב-`docs/plans/sync/ROADMAP.md`.)
9. **`index.html` ‏בבעלותנו**, ‏לפחות ‏עד ‏סוף R5 — Vite ‏פולט ‏צרורות ‏בשמות ‏קבועים
   ‏וה-HTML ‏נשאר ‏כתוב ‏ביד. ‏ההערות ‏בו ‏הן ‏תיעוד ‏נושא-משקל ‏שנכתב ‏לאורך ‏כמה ‏סלייסים.
10. **‏גרסת Obsidian ‏מוצמדת ‏ל-1.12.7.** ‏ראה ‏הרשומה ‏למטה. ‏זהו ‏אילוץ ‏חוצה-קווים —
    ‏הוא ‏חל ‏גם ‏על ‏קו ‏הסנכרון ‏ועל ‏כל ‏עבודה ‏עתידית ‏ב-`client-mobile`.

### ‏אילוץ ‏חיצוני — ‏שער-ההפעלה ‏של Obsidian 1.13 (‏נמדד 2026-08-02)

**‏מה ‏קרה**: Obsidian 1.13.4 (2026-07-30) ‏הוסיפה ‏בדיקה ‏בהפעלה. `App.getInfo()` ‏של
Capacitor ‏חייב ‏להחזיר ‏שדה `terms` ‏שתוכנו ‏הצהרה ‏על ‏אי-הפצת ‏האפליקציה ‏בלי ‏אישור
‏מפורש; ‏אחרת ‏הבאנדל ‏זורק `throw new Error` **‏ריק** ‏ולא ‏עולה. ‏נוסף ‏גם `getManagedPolicy()`
(‏מדיניות ‏ארגונית), ‏אך ‏הוא ‏עטוף ‏ב-try/catch ‏ולכן ‏אינו ‏קטלני.

**‏מה ‏נמדד** — ‏בקרה ‏נקייה, ‏אותה ‏כספת ‏רשומה ‏ואותה ‏המתנה:

| | 1.12.7 | 1.13.4 |
|---|---|---|
| ‏עלייה ‏בדפדפן | ✅ ‏בוחר-הכספות ‏מרונדר ‏במלואו | ❌ ‏תקוע ‏על "Loading Obsidian mobile (14/14)" |
| ‏שגיאות `app.js` ‏בקונסולה | 0 | 4 |
| `native-bridge.js` | — | **‏זהה ‏בייט-בייט** |
| ‏סט ‏פלאגיני Capacitor | 11 | **‏אותם 11** |
| ‏שערי `terms` ‏בבאנדל | — | **‏בדיוק ‏אחד** |

**‏המסקנה**: ‏הצינור ‏שלנו ‏תקין — ‏ההורדה, ‏החילוץ, ‏השימים ‏והטסטים ‏כולם ‏עוברים ‏על 1.13.4.
‏חוזה ‏הגשר ‏לא ‏זז ‏בכלל. ‏השער ‏הוא ‏החסם ‏היחיד, ‏והוא ‏מכוון ‏ולא ‏באג.

**‏ההחלטה**: ‏נשארים ‏על **1.12.7**. ‏השים ‏**‏אינו ‏מכיל** ‏את ‏המחרוזת — ‏הוא ‏קורא ‏ערך
‏שמפעיל ‏מספק ‏בזמן ‏ריצה (`localStorage`/`window`), ‏וברירת ‏המחדל ‏ריקה, ‏כלומר ‏השער
‏חוסם ‏כברירת ‏מחדל. **‏אין ‏לפתור ‏את ‏זה ‏ע"י ‏הטמעת ‏ההצהרה ‏בקוד** — ‏זו ‏החלטה, ‏לא ‏חוב ‏טכני.

**‏השלכה ‏על ‏הרצף**: ‏אין. ‏אף ‏סלייס ‏ב-R ‏אינו ‏תלוי ‏בגרסת Obsidian. ‏אבל ‏כל ‏בריף ‏שנוגע
‏ב-`client-mobile` ‏צריך ‏להניח 1.12.7 ‏כבסיס-מדידה, ‏ולא ‏"‏האחרונה".

### ‏סדר ‏העבודה ‏ורמת-האימות (‏הוכרע 2026-08-02)

**‏הסדר**: **R-lint → R2 → R1**. ‏הלינטר ‏זול ‏וסוגר ‏חוב; R2 ‏פותח ‏את ‏כל ‏השאר;
R1 ‏הקשה ‏ביותר ‏ונכנס ‏אחרון, ‏כשהתשתית ‏יציבה.

| ‏סלייס | ‏צינור |
|------|------|
| **R-lint** | ‏אליעזר + ‏כלב ‏בלבד — ‏אין ‏החלטות ‏ארכיטקטוניות, ‏הכל ‏מדוד ‏מראש |
| **R2** | ‏צינור ‏מלא (‏אביגיל → ‏אליעזר → ‏כלב) — ‏משנה ‏איך ‏המוצר ‏נבנה |
| **R1** | ‏צינור ‏מלא. **‏סבב ‏אביגיל ‏חמישי, ‏לא ‏פיצול ‏נוסף** — ‏הבריף ‏כבר ‏פוצל ‏פעם ‏מ-R0, ‏ופיצול ‏נוסף ‏יוריד ‏אותו ‏מתחת ‏לגודל ‏שבו ‏הוא ‏עוד ‏מוכיח ‏משהו |

---

## ‏הרצף

### R0 — `monorepo-foundation` ✅ ‏בוצע, ‏ממתין למיזוג
**‏מה נתן**: workspaces ‏עם bun · `packages/core` ‏עם tsconfig project-references ·
‏סקריפט-גבול נאכף · `toRelative` ‏על 7 ‏טסטים ‏כהוכחת-צינור · `bunfig.toml` ‏שמציל
‏את מסלול ה-npm.
**‏מצב**: 9 ‏סבבי אביגיל (71 ‏ממצאים) → READY. ‏בוצע ב-`slice/r0-qwen`, 4 ‏קומיטים,
‏אפס נגיעה ב-`src/`. **‏ממתין ל-כלב ‏ולמיזוג ל-dev.**

### R1 — `core-resolve-safe` 🟡 ‏בריף טיוטה, ‏לא מאומת
**‏מטרה**: `resolveSafe` — ‏שומר ה-path-traversal — ‏קיים ‏ב**‏ארבעה** ‏מימושים לא-זהים
‏בשתי חבילות. ‏אחרי הסלייס יש **‏אחד**, ‏ב-`@ow/core`, ‏עם טסטים.
**‏בחוץ**: ‏כתיבת `toRelative` ‏מחדש (‏מגיעה מ-R0) · ‏העברת חבילות ל-`packages/`.
**‏תלוי ב**: R0 ‏מוזג.
**‏מצב**: ‏4 ‏סבבי אביגיל, ‏ובכל אחד **‏התיקון ייצר רגרסיה חדשה** — *"‏התיקון סוגר את
‏השדה שנבדק ומשאיר את השדה שלידו"*. ‏הבריף הנוכחי ‏**‏לא עבר אימות בצורתו הזו**.
**‏למה הוא ראשון**: ‏ארבעת האתרים נבדלים זה מזה ב-7 ‏ממדים נמדדים (§3 ‏בבריף) —
‏זהו המקרה הקשה, ‏והוא גם היחיד שהוא **‏גבול-אבטחה**.
**‏מגבלה שצריך לומר בקול**: ‏כל ארבעת האתרים הם Node. **R1 ‏לא מוכיח שהליבה
‏חוצה runtime.**

### R2 — ‏שלב-בנייה ל-`client-mobile` ✅ **‏הוכרע 2026-08-02**
**‏ההחלטה**: **Vite 8 ‏עם Rolldown**, ‏שרת-הפיתוח ‏**‏כבוי**, `vite build --watch` ‏בלבד.
`src/runtime-server/server` ‏ממשיך להגיש את הקבצים ואת `/api/*` ‏כמו היום — ‏אין שרת
‏שני, ‏אין proxy, ‏אין הבדל בין dev ‏לפרודקשן. HMR ‏לא בשימוש ‏(‏הוא ממילא חסר-משמעות
‏כאן: ‏המודולים מתקינים `window.require` ‏וגלובלים ‏**‏לפני** ‏שהחבילה של Obsidian נטענת).

**‏נמדד ‏בפועל** (`/tmp/vite-bun-probe`, Vite 8.2.0):

| ‏מה נבדק | ‏תוצאה |
|---------|-------|
| `bun run vite build` (‏מריץ ‏תחת Node) | ✅ exit 0. `vite/8.2.0 linux-x64 node-v25.9.0` |
| `bun --bun run vite build` (‏תחת ‏ריצת Bun) | ✅ exit 0, `bun-v24.3.0`, **‏פלט זהה byte-for-byte** ‏ל-Node |
| `vite build --watch` | ✅ ‏בנייה-מחדש ‏ב-13ms; ‏ה-hash ‏בשם-הקובץ ‏משתנה |
| ‏ריבוי נקודות-כניסה | ✅ `build.rolldownOptions.input` |
| ‏פלט **‏סקריפט קלאסי** (IIFE) | ✅ `var __owCore=(function(e){…})({})` |

**‏המסקנה המבצעית ‏החשובה ‏ביותר** — ‏פלט ה-IIFE: ‏מודול TS ‏מקומפל ‏לסקריפט **‏קלאסי**
‏שנתלה על גלובל, ‏כלומר **‏בדיוק ‏החוזה ‏הקיים ‏של `window.__ow*`**. ‏לכן R4 ‏יכול להמיר
‏מודולים ‏ל-TS ‏**‏בלי לגעת ‏ב-`index.html` ‏בכלל** ‏בשלב הראשון — ‏המעבר ל-`type="module"`
‏הוא צעד נפרד ‏ומאוחר, ‏לא תנאי-כניסה.

**bun**: ‏נשאר ‏מנהל-החבילות ‏ומריץ-המשימות (‏כפי ש-R0 ‏קבע). ‏Vite ‏עצמו ‏ירוץ ‏**‏תחת Node** —
‏כלומר `bun run vite build`, ‏**‏לא** `bun --bun`. ‏זאת ‏למרות ‏ששתיהן ‏עברו ‏במדידה:
VoidZero ‏הצהירו ‏ש-Bun ‏**‏כ-runtime** ‏אינו ‏יעד ‏נתמך (‏כמנהל-חבילות ‏— ‏כן), ‏ויש ‏באג
‏פתוח ‏שבו `undefined` ‏ב-`define` ‏נופל ‏על ‏napi ‏מחמיר ‏של Bun (oven-sh/bun#26388).
‏אין ‏סיבה ‏לקחת ‏סיכון ‏על ‏נתיב ‏לא-נתמך ‏עבור ‏אפס ‏רווח ‏מדיד.

**‏מה ‏שנשאר ‏פתוח ‏ל-R2**: ‏מי ‏הבעלים ‏של `index.html`. ‏ההמלצה — ‏**‏אנחנו**, ‏לפחות ‏עד ‏סוף R5:
Vite ‏פולט ‏צרורות ‏בשמות ‏קבועים ‏וה-HTML ‏נשאר ‏כתוב ‏ביד ‏עם ‏ההערות ‏שלו, ‏שהן ‏תיעוד
‏נושא-משקל ‏שנכתב ‏לאורך ‏כמה ‏סלייסים ‏מאומתים.

**‏רצפת Node — ‏הוכרע 2026-08-02**: Vite 8 ‏דורש **Node 20.19+ / 22.12+**, ‏והפרויקט
‏מצהיר ‏היום "Node 18+ or Bun". **‏מרימים ‏את ‏הרצפה ‏ל-20.19+. ‏רצפה ‏אחת, ‏לא ‏שתיים.**
‏נשקלה ‏הפרדה ‏בין ‏רצפת-ריצה ‏לרצפת-בנייה ‏ו**‏נדחתה** ‏במפורש: ‏שתי ‏רצפות ‏הן ‏שתי
‏הצהרות ‏שצריך ‏לתחזק ‏ולבדוק ‏בנפרד, ‏והריפו ‏הזה ‏כבר ‏שילם ‏מחיר ‏על ‏הצהרות-תיעוד
‏שנסחפו (`docs-truth-final`, `zero-patches`). ‏מי ‏שתקוע ‏על Node 18 ‏יתמודד.
**‏נגזרת**: `README.md` ‏ו-`AGENTS.md` ‏מתעדכנים ‏ב-R2 — ‏הבריף ‏של R0 ‏סימן ‏"‏רצפת Node"
‏כסיכון ‏ודחה ‏בכוונה ‏נגיעה ‏ב-README, ‏וזה ‏החריץ ‏שהושאר.

### R3 — ‏רשימת-ההיתר של הפרוקסי ‏לליבה
**‏מטרה**: `isAllowed` ‏קיים ‏בשני עותקים — `src/runtime-server/server/api/proxy.js`
(Node, CJS) ‏ו-`src/deployments/cloudflare/proxy-worker.js` (Worker, ESM). ‏אחרי
‏הסלייס יש אחד.
**‏למה הוא הבא**: ‏זהו **‏גבול-SSRF**, ‏הוא טהור לחלוטין (`URL` ‏ומחרוזות בלבד),
‏והוא **‏הצרכן הראשון של הליבה שאינו Node** — ‏כלומר הוא המקום שבו החלטת R2
‏נבחנת בפועל, ‏בקוד קטן ‏שקל לגבות.
**‏הסתייגות ‏שנרשמה 2026-08-02**: ‏ה-Worker ‏**‏אינו host ‏של ‏האפליקציה** — ‏הפריסה
‏ב-Cloudflare ‏היא client-only (OPFS), ‏וה-Worker ‏קיים ‏אך ‏ורק ‏עבור `/api/proxy-request`
(‏פרוקסי-CORS ‏להתקנת ‏פלאגינים), 337 ‏שורות ‏בסך ‏הכל. ‏לכן ‏הסלייס ‏הזה ‏הוא ‏**‏קטן
‏ומבחני**, ‏לא ‏אבן-דרך ‏ארכיטקטונית. ‏אם ‏מסתבך — ‏לגיטימי ‏להשאיר ‏את ‏השכפול ‏ולהמשיך ‏ל-R4.
**‏נמדד**: ‏שני העותקים ‏**‏עדיין זהים פונקציונלית** (‏ההבדל היחיד הוא הערות ‏ורשומה
‏כפולה של `raw.githubusercontent.com` ‏בעותק ‏ה-Node — ‏חסרת-השפעה, `Set`).
‏כלומר **‏עוד לא נסחפו** — ‏זה הזמן לאחד, ‏לפני שהם נסחפים כמו `resolveSafe`.
**‏תלוי ב**: R2 (‏מסלול ‏א' ‏או ב'). ‏במסלול ‏ג' — ‏הסלייס יורד.

### R4 — ‏המודולים הטהורים שכבר קיימים, ‏לתוך הליבה
**‏מטרה**: ‏בריפו כבר קיים דפוס "‏מודול טהור" — ‏קבצים שנכתבו ‏כ-IIFE ‏שנתלית
‏על `window.__ow*` ‏כדי שיהיו ‏גם ‏ניתנים-לטעינה ‏ב-`node:test` ‏וגם ‏ב-`<script>`.
‏זה מודול-סיסטם ידני. R0 ‏נתן ‏מודול-סיסטם אמיתי; ‏כאן ממירים.
**‏מועמדים נמדדים** (‏אפס עד שתי הפניות חיצוניות כל אחד):
`sync/decide-pull.js` (75, ‏אפס) · `shims/path.js` (120, ‏אפס) ·
`vault-root-path.js` (43) · `bootstrap-lookup.js` (61) · `cache-invalidation.js` (67).
**‏הרווח האמיתי**: ‏לא השורות — ‏אלא ‏מחיקת ‏שורות מסדר-הטעינה ‏ב-`index.html`,
‏שהוא היום גרף-תלויות ‏מתוחזק-ביד ‏עם 25 ‏צמתים ‏והערות ‏מסוג "‏חייב להיטען לפני boot.js".
**‏בחוץ**: `sync/decide-pull.js` ‏אם קו-הסנכרון ‏כבר החליף אותו (‏החלטה 5).
**‏תלוי ב**: R2 + R3.

### R5 — ‏חילוץ החלטות מתוך שלושת הקבצים הגדולים
**‏מטרה**: `boot.js` (1592) · `shims/capacitor-shim.js` (1151) · `shims/electron.js` (892) —
‏יחד 3,635 ‏שורות, 56% ‏מ-`client-mobile`. ‏להוציא מהם את ה**‏החלטות** (‏מה לעשות)
‏ולהשאיר את ה-**‏ביצוע** (‏לעשות) ‏במקום.
**‏למה לא קודם**: ‏אין כאן שכפול-מדיד ‏שמצדיק ‏את הסיכון, ‏והם קוד-ריצה חי ‏ללא
‏שער-רגרסיה משמעותי. ‏זה הסלייס שהכי קל לשבור בו את המוצר בשקט.
**‏תלוי ב**: R4 (‏הדפוס מוכח בקטן) · **R-browser-gate** — ‏חסם ‏קשיח, ‏ראה ‏למטה.

### R6 — ‏צד-השרת
**‏מטרה**: `api/bootstrap.js` (585) ‏ו-`api/fs.js` (509) — ‏חילוץ תכנון טהור
‏מהריתוך ‏ל-Express. ‏זו בדיוק הפתולוגיה ש-`sync-server/resolve-safe.js` ‏מתעד ‏על עצמו:
*"coupled to `req`"* ‏— ‏ולכן שוכפל.
**‏תלוי ב**: R1.

### R-browser-gate — ‏בדיקות ‏דפדפן ‏🔴 **‏חוסם ‏את R5**, ‏אינו ‏חסום ‏בעצמו
**‏הבעיה**: ‏אין ‏היום ‏**‏שום** ‏בדיקה ‏אוטומטית ‏שמריצה ‏את ‏המוצר ‏בדפדפן. ‏כל ‏אימות
‏הדפדפן ‏עד ‏כה ‏נעשה ‏**‏ידנית ‏ע"י ‏כלב** ‏(‏ראה ‏למשל `desktop-layout-now` Commit 6,
"‏אימות ‏מלא ‏בדפדפן"). ‏זה ‏עבד ‏כל ‏עוד ‏השינויים ‏היו ‏קטנים ‏ומקומיים.
**‏למה ‏זה ‏נהיה ‏חוסם ‏עכשיו**: R5 ‏נוגע ‏ב-`boot.js` + `capacitor-shim.js` + `electron.js`
— **3,635 ‏שורות, 56% ‏מ-`client-mobile`**, ‏קוד ‏ריצה ‏חי ‏שכל ‏המוצר ‏תלוי ‏בו.
‏אימות ‏ידני ‏לא ‏מכסה ‏שינוי ‏בהיקף ‏כזה, ‏ו-DoD#7 (`git diff` ‏על `src/` ‏ריק) —
‏השער ‏שהחליף ‏אותו ‏ב-R0 — ‏לא ‏חל ‏כאן ‏בהגדרה, ‏כי ‏R5 ‏**‏כן** ‏משנה ‏את `src/`.
**‏מטרה**: ‏שער ‏אוטומטי ‏שמעלה ‏את ‏המוצר ‏בדפדפן ‏אמיתי ‏ומאמת ‏את ‏מסלולי-הליבה —
‏כספת ‏נטענת · ‏פתק ‏נפתח ‏ונערך ‏ונשמר · ‏עץ-הקבצים ‏מציג · ‏חיפוש ‏עובד ·
‏אין ‏שגיאות ‏קונסולה ‏חדשות.
**‏תלוי ‏ב**: ‏כלום. ‏אפשר ‏לכתוב ‏אותו ‏במקביל ‏לכל ‏השאר.
**‏להכריע ‏בבריף**: ‏באיזה ‏כלי (‏בריפו ‏כבר ‏קיימת ‏מיומנות Playwright ‏דרך ‏הסקילים) ·
‏מול ‏איזו ‏פריסה ‏רצים (‏שרת Node ‏מקומי ‏או ‏בניית CF) · ‏מה ‏עושים ‏עם ‏הזמן — ‏החבילה
‏של Obsidian ‏נטענת ‏מהרשת ‏ואיטית, ‏ולכן ‏השער ‏עלול ‏להיות flaky.

**‏🟢 ‏זרע ‏קיים ‏כבר ‏בקוד** (2026-08-02): ‏ה-boot watchdog ‏ב-`boot.js` ‏עושה ‏בקטן ‏בדיוק ‏את
‏מה ‏שהסלייס ‏הזה ‏צריך ‏בגדול — ‏הוא ‏עונה ‏על ‏השאלה **"‏האם ‏אובסידיאן ‏באמת ‏רונדרה?"**
‏באמצעות `BOOT_RENDERED_SELECTORS` (`.workspace, .mobile-vault-chooser-screen,
.mobile-onboarding`), ‏שהם ‏איחוד ‏שני ‏ה-selectors ‏ששתי ‏זרימות ‏ההמתנה ‏כבר ‏משתמשות ‏בהן.
‏זהו ‏אות-ההצלחה ‏שהבריף ‏יצטרך ‏ממילא. ‏גם ‏הטיימינג ‏כבר ‏פתור: ‏הדדליין ‏נספר ‏מ-`onload`
‏של `app.js` ‏ולא ‏מרגע ‏ההזרקה, ‏אחרת ‏רשת ‏איטית ‏מייצרת ‏התראות-שווא — ‏אותה ‏מלכודת
‏flakiness ‏שנרשמה ‏למעלה. **‏להתחיל ‏מהשורות ‏האלה, ‏לא ‏מדף ‏חלק.**
**‏הערה**: ‏זהו ‏הסלייס ‏היחיד ‏ברשימה ‏שערכו ‏חורג ‏מקו ‏ה-restructure — ‏הוא ‏משרת ‏גם
‏את ‏קו ‏הסנכרון (S5) ‏וגם ‏כל ‏שינוי ‏עתידי ‏ב-`client-mobile`.

### R-lint — ‏לינטר ‏🟢 ‏עצמאי, ‏לא ‏חוסם ‏ולא ‏חסום
**‏מטרה**: ‏להכניס ‏לינטר ‏לריפו. ‏**‏אין ‏אחד ‏היום ‏בכלל** — ‏נמדד: ‏אין `.eslintrc`,
‏אין `.prettierrc`, ‏אין `.editorconfig`, ‏אין `.oxlintrc`. ‏בשורש ‏רק `test` ‏ו-`typecheck`.
**‏הכלי**: **oxlint ‏עצמאי** ‏עם `.oxlintrc.json` — ‏לא ‏דרך Vite+ (‏ראה ‏למטה).
**‏תלוי ‏ב**: ‏כלום. ‏אינו ‏תלוי ‏ב-R2 ‏ואינו ‏חוסם ‏אותו.

**‏נמדד ‏2026-08-02** (`bunx oxlint@latest src/ packages/ scripts/`, 97 ‏קבצים):

| | |
|---|---|
| ‏סה"כ | **84 ‏ממצאים, ‏אפס ‏שגיאות** |
| `no-unused-vars` | 81 — ‏רובם ‏המוחלט `catch (_) {}` ‏ופרמטרים ‏לא-בשימוש |
| `no-unused-expressions` | 3 |

‏זה ‏נקי ‏באופן ‏חריג ‏ל-10,000 ‏שורות ‏שנכתבו ‏בלי ‏לינטר ‏מעולם. ‏עם
`caughtErrorsIgnorePattern: '^_'` (‏האידיום ‏הוא ‏בחירת-סגנון ‏מכוונת ‏בריפו)
‏נשארים ‏בערך ‏20 ‏ממצאים ‏אמיתיים. **‏אין ‏כאן ‏סלייס-טריאז' — ‏זו ‏ישיבה ‏אחת.**

**‏החלטה ‏שצריך ‏לקבל ‏בבריף**: ‏מה ‏קורה ‏ל-`scripts/check-core-boundary.mjs` — 101
‏שורות ‏של ‏לינטר-גבול ‏שנכתב ‏ביד ‏ב-R0. ‏האם `no-restricted-imports` ‏של oxlint ‏מכסה ‏את
‏שני ‏הכללים (‏תלות ‏לא-מוצהרת · ‏בריחה ‏מחוץ ‏לחבילה), ‏או ‏שהסקריפט ‏נשאר. **‏אזהרה**:
‏הסקריפט ‏מחשב `relative()` ‏על ‏הנתיב ‏**‏המפוענח** ‏ולא ‏על ‏טקסט ‏הספסיפייר (‏כלב ‏אימת
‏שזה ‏מה ‏שתופס ‏גם `./../../x`) — ‏החלפה ‏בכלל ‏מבוסס-טקסט ‏תהיה ‏רגרסיה ‏שקטה.

**🔴 ‏פורמטר — ‏מחוץ ‏להיקף, ‏בכוונה.** `bunx oxfmt --check` ‏נמדד: ‏**89 ‏מתוך 97 ‏קבצים**
‏היו ‏משתנים. ‏שתי ‏סיבות ‏לא ‏לעשות ‏את ‏זה ‏עכשיו:
1. **‏ארבעה ‏ענפים ‏לא-ממוזגים ‏ושמונה worktrees** (`demo-and-docs-truth`,
   `sync-server-cors`, `preview/ff-desktop`, `zero-patches-preview`) — ‏ריפורמט ‏גורף
   ‏מתנגש ‏עם ‏כולם, ‏כמעט ‏בכל ‏קובץ.
2. **‏הוא ‏שובר ‏את ‏שער-הבטיחות ‏של ‏השיטה.** DoD#7 ‏ב-R0 ‏היה `git diff --stat dev...HEAD
   -- src/` ‏**‏ריק**, ‏והבריף ‏מנמק: *"‏אם git diff ‏על src/ ‏ריק, ‏מתמטית ‏אי-אפשר ‏היה
   ‏לשבור ‏כלום"*. ‏ריפורמט ‏גורף ‏מרוקן ‏את ‏השער ‏הזה ‏מתוכן ‏לכל ‏מה ‏שיבוא ‏אחריו, ‏והורג
   ‏את `git blame` ‏בדיוק ‏כשנכנסים ‏לקו ‏שהבריפים ‏בו ‏נשענים ‏על ‏קריאת ‏היסטוריה.

‏אם ‏בכל ‏זאת ‏רוצים ‏פורמטר — ‏הדפוס ‏הבטוח ‏הוא ‏הפעלה ‏**‏רק ‏על ‏קבצים ‏שנגעו ‏בהם ‏ממילא**,
‏כך ‏שהוא ‏נכנס ‏בהדרגה ‏עם R4/R5 ‏ולא ‏בפיצוץ ‏אחד.

---

## ‏מה **‏לא** ‏ברצף

- **‏המרה סיטונאית ‏של `src/` ‏ל-TS.** 10,000 ‏שורות, ‏אפס טסטי-רגרסיה בדפדפן,
  ‏ותשואה שיורדת ‏ככל שהקובץ ‏יותר IO. ‏ההמרה היא **‏תוצר-לוואי** ‏של החילוץ, ‏לא מטרה.
- **`src/sync-server` ‏ו-`src/client-mobile/sync`** — legacy (‏החלטה 5).
- **‏העברת חבילות ‏ל-`packages/`** — ‏החלטה נפרדת (‏החלטה 6).
- **`sw.js` ‏ו-`vendor/`** — ‏האחד ‏service worker ‏עם חוזה-דפדפן משלו, ‏השני
  ‏קוד ‏של Obsidian ‏שלא נוגעים בו ‏מעצם התזה של הפרויקט.

---

## ‏שאלות פתוחות

| # | ‏שאלה | ‏מתי צריך תשובה |
|---|------|----------------|
| ~~1~~ | ~~‏מסלול ה-emit~~ | ✅ ‏נסגר 2026-08-02 — Vite 8 + Rolldown, ‏שרת-פיתוח כבוי |
| ~~2~~ | ~~‏האם `client-mobile` ‏מקבל שלב-בנייה~~ | ✅ ‏נסגר ‏עם 1 |
| 3 | ‏האם החבילות הקיימות ‏(‏שנשארות JS) ‏מקבלות `checkJs`/JSDoc, ‏או שהטיפוסים ‏נעצרים ‏בגבול הליבה | ‏לפני R3. **‏נסגר ‏במדידה, ‏לא ‏בהחלטה** — ‏להריץ `checkJs` ‏ולספור ‏רעש, ‏כמו ‏שנעשה ‏בלינטר |
| ~~4~~ | ~~‏שער-רגרסיה בדפדפן~~ | ✅ ‏נסגר ‏2026-08-02 — ‏הפך ‏לסלייס ‏משלו, **R-browser-gate** ‏למעלה |
| ~~5~~ | ~~R1: ‏סבב ‏חמישי ‏או ‏פיצול~~ | ✅ ‏נסגר ‏2026-08-02 — **‏סבב ‏אביגיל ‏חמישי** |
| ~~6~~ | ~~‏בעלות ‏על `index.html`~~ | ✅ ‏נסגר ‏2026-08-02 — ‏אנחנו (‏החלטה 9) |
| ~~7~~ | ~~‏לינטר~~ | ✅ ‏נסגר ‏2026-08-02 — oxlint ‏עצמאי, ‏רשומת **R-lint** ‏למעלה. ‏נותר ‏פתוח ‏בתוכה ‏רק ‏גורל `check-core-boundary.mjs` |

### Vite+ — ‏נבדק ‏2026-08-02, ‏**‏לא ‏מאומץ ‏כרגע**

Vite+ ‏הוא ‏שכבת-איחוד ‏מעל Vite 8 (‏עם Vitest, Oxlint, Oxfmt, tsdown ‏ומריץ-משימות
‏עם ‏מטמון), ‏MIT, ‏**‏בשלב beta ‏מאז 2 ‏ביולי 2026**. ‏מדוע ‏לא ‏עכשיו:

- **`vp dev`** — ‏שרת-פיתוח ‏עם HMR. ‏זה ‏בדיוק ‏מה ‏שהחלטנו ‏**‏לכבות**. ‏אפס ‏ערך ‏כאן.
- **`vp test`** — Vitest. ‏הריפו ‏מריץ `node --test` ‏בחלק ‏מהחבילות ‏ו-`bun test` ‏באחרות,
  ‏בכוונה (§Requirements ‏ב-README). ‏המרה ‏היא ‏סלייס ‏בפני ‏עצמו ‏עם ‏תשואה ‏לא ‏ברורה.
- **`vp run`** — ‏מריץ-משימות ‏עם ‏מטמון. R0 ‏בדיוק ‏קבע `bun run --filter '*' test`.
  ‏ב-5 ‏חבילות ‏המטמון ‏חוסך ‏שניות. ‏שולי.
- **`vp check`** — ‏כאן ‏יש ‏ערך ‏אמיתי: ‏**‏אין ‏בריפו ‏לינטר ‏או ‏פורמטר ‏בכלל** (‏נמדד),
  ‏ו-`check-core-boundary.mjs` ‏הוא ‏לינטר-גבול ‏שנכתב ‏ביד, 101 ‏שורות.
  ‏אבל ‏אפשר ‏לאמץ ‏**oxlint ‏לבדו** ‏בלי Vite+ ‏בכלל — ‏ראה ‏שאלה 7.

**‏השורה ‏התחתונה**: Vite+ ‏הוא ‏**‏שכבה ‏מעל** Vite 8, ‏ולכן ‏אימוץ ‏שלו ‏מאוחר ‏יותר
‏אינו ‏דורש ‏לחזור ‏אחורה ‏על ‏שום ‏החלטה ‏כאן. ‏אין ‏סיבה ‏להשעין ‏קו-עבודה ‏רב-סלייסים
‏על ‏טולצ'יין ‏בן ‏חודש ‏ב-beta ‏כשהרווח ‏היחיד ‏הנוכחי ‏זמין ‏גם ‏בנפרד.

---

## ‏ניקוי ‏תפעולי (‏אושר 2026-08-02, ‏טרם ‏בוצע)

‏שני ‏ענפים ‏למחיקה ‏יחד ‏עם ‏ה-worktrees ‏שלהם:

- **`slice/sync-server-cors`** (20/7) — ‏הוכרע ‏שלא ‏ימוזג (‏החלטה 9 ‏ב-`docs/plans/sync/ROADMAP.md`:
  `src/sync-server` ‏נשאר legacy). ‏מחיקה ‏ישירה.
- **`slice/demo-and-docs-truth`** (27/7) — ‏**‏לאמת ‏לפני ‏המחיקה** ‏שתוכנו ‏אכן ‏נגזר ‏ע"י
  `docs-truth-final` ‏ו-`demo-text-polish` ‏שמוזגו. ‏לא ‏למחוק ‏על ‏סמך ‏התאריך ‏בלבד.

---

## ‏יחס לקו-הסנכרון (S)

S1 (`sync-core-pure`) ‏כותב ‏חמישה מודולים טהורים ‏ל-`packages/core/sync` ‏ותלוי ‏**‏רק
‏ב-R0**. ‏כלומר קו-הסנכרון ‏הוא כבר לקוח של הליבה, ‏והוא **‏אינו חסום** ‏על R1-R6.
‏אבל S5 (‏חיווט ל-obsidian-web) ‏ו-S6 (‏פלאגין דסקטופ) ‏צורכים ליבה **‏מחוץ ל-Node** —
‏ולכן הם נתקלים ‏באותו ‏חסם ‏שמתואר ב-R2. **‏החלטת R2 ‏משרתת את שני הקווים.**
