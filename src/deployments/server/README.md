# obsidian-web — server deployment (Node.js)

פריסת-שרת (node רגיל; אין Dockerfile בריפו כיום); הקוד ב-`src/runtime-server/server`.
HTTPS+auth ע"י reverse-proxy חיצוני (Caddy מומלץ).

ראה `README.md` (שורש הריפו), סעיף "Node.js deployment", לפרטי setup מלאים.
